import React from 'react';
import Default from '../layouts/Default';

const Checkout = () => {
    return (
        <Default>
            Checkout Page
        </Default>
    );
};

export default Checkout;